#include "src/tp.h"
#include "src/abb.h"
#include <stdio.h>
#include <stdlib.h>


// typedef struct comando {
// 	void* simbolo;
// } comando_t;


// typedef struct menu {
//     lista_t* comandos; 
// } menu_t;


// menu_t* menu_crear() {
// 	menu_t* menu = malloc(sizeof(menu_t));
// 	if (!menu) {
// 		return NULL;
// 	}

// 	menu->comandos = lista_crear();

// 	return menu;
// }

// void agregar_comando(menu_t* menu, void* simbolo, vo) {



// 	lista_insertar(menu->comandos, );
// }


// void ejecutar_comando(menu_t* menu, voi)








int main(int argc, char const *argv[])
{
	//Se recomienda pasar el archivo de texto con los pokemon como argumento al ejecutar el programa

	//crear el juego
	tp_crear(argv[1]);
	//mostrar menues
	//correr carrera
	//etc




	return 0;
}
